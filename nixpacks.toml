[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r backend_estacionamiento/requirements.txt"]

[start]
cmd = "cd backend_estacionamiento/backend && uvicorn servidor:app --host 0.0.0.0 --port $PORT"
